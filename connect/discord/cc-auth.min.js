// Lightweight loader for cc-auth.js (keeps compatibility with pages that expect cc-auth.min.js)
(function(){
  var s = document.createElement('script');
  s.src = '/connect/discord/cc-auth.js';
  s.defer = true;
  document.head.appendChild(s);
})();
